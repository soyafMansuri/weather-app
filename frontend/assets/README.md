# assets/

Place any static assets here (custom fonts, favicon, extra icons, etc.).

Weather icons are currently loaded live from OpenWeather's CDN
(`https://openweathermap.org/img/wn/{icon}@2x.png`), so no local
icon assets are required to run the app.
